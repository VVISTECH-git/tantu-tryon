{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ] && [ -n \"$DATABASE_URL\" ]; then pnpm --filter @tantu/web db:migrate; fi && pnpm --filter @tantu/web build",
  "outputDirectory": "apps/web/.next"
}
